"use client";

export default function WorkflowsPage() {
  return (
    <div className="flex items-center justify-center h-64">
      <p style={{ color: "var(--text-muted)" }}>No workflows configured.</p>
    </div>
  );
}
